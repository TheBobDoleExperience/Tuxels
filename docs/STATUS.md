# Tuxels — Current Status

**One-paragraph summary:** Milestone **M0 shipped** as `v0.0.1-m0` on 2026-04-20 (commit `be87b70`). **M1 shipped** as `v0.1.0-m1` on 2026-04-20 (130 tests). **M2 active** (Position, Shape, Stroke Quality) — S0 landed 2026-04-20: `LayerBase` now carries `int originX/originY`, `PixelLayer::renderTile` folds mask sampling into its own origin-aware path, `compose()` is origin-agnostic, `CropCommand` computes per-layer doc-space overlap (layers with no overlap drop to empty), `.txl` bumped to v2 with per-layer dims + origin (v1 still readable), `Document::addPixelLayer` variant unlocks S1 Place Image. S1 landed 2026-04-20: `File → Place…` (Ctrl+Shift+P) imports a PNG as a new layer centered in the active doc, wrapped in the `LayerOpCommand` closure pattern, oversized images keep offscreen pixels. S2 landed 2026-04-20: Move tool (V) drags the active layer's origin; live drag writes origin directly on the layer so the existing dirty-rect path repaints only the vacated + newly-filled regions; release produces a `MoveLayerCommand` (id-based lookup) that swaps origin on undo/redo. S3 landed 2026-04-20: Free Transform (Ctrl+T) — bilinear `resampleBilinear` + `Affine2D` in new `src/geom/`, `LayerOverride` + compose overloads for zero-mutation preview, `TransformCommand` bundles pixel + origin swap and looks up by id, `TransformTool` is a modal state machine with 4 corner-handle scale / interior-translate / exterior-rotate drag regions; Enter commits, Escape cancels, tool switch auto-commits; CanvasView renders the bbox + handle dots; ToolsPanel "T" button + Edit menu entry. S4 landed 2026-04-20: `SelectionMask::fillPolygon` even-odd scanline rasterizer (strict-interior edge rule: pixel center must lie strictly inside both scan intersections, so integer-vertex polygons still yield predictable axis-aligned rects); `LassoTool` (L) freehand drag with 1-doc-px chord decimation; `PolyLassoTool` click-vertex with rubber-band rendering, closes on near-start click / Enter, cancels on Escape; shared combine-mode options row. S5 landed 2026-04-20: `SelectByColorTool` (non-contiguous wand) walks the active layer's present tiles in layer-local coords, marks every pixel within L∞ tolerance of the seed, commits a `SelectionCommand` with the shared combine-mode plumbing; `channelDist` promoted from `FloodFill.cpp` to the header so wand + SBC share the same metric; ToolsPanel shares the wand options row, Shift+W cycles the pair. S6 landed 2026-04-20: brush dynamics — `RoundBrushParams` gains `sizeJitter` / `opacityJitter` / `pressureMultiplier` (pressure is an unwired tablet stub), `BrushEngine` carries a stroke-scoped `std::mt19937` seeded from a monotonic stroke id (tests can pin via `setNextStrokeSeedForTesting`), `applyStamp` branches on jitter so zero-jitter output is bitwise identical to pre-S6; jittered path builds a scratch kernel via new static `RoundBrush::buildKernel` at effective diameter `= round(D·(1 + U(-j,+j)))` capped at 2×D with opacity clamped to [0,1]. ToolsPanel brush options row adds Size Jitter / Opacity Jitter / Spacing sliders (spacing 1–100 % maps to `spacingRatio ∈ [0.01, 1.0]`). 214 tests green (+7 in `test_brush_dynamics`). Plan: `/home/james/.claude/plans/cryptic-stargazing-moonbeam.md`.

## Active Milestone: M2 — Position, Shape, Stroke Quality

**Definition of Done:** Import additional images into an existing doc (Place), move + free-transform layers, lasso / polygonal-lasso / select-by-color selection tools, brush dynamics (size / opacity jitter + spacing), and a `.txl` round-trip that preserves layer origins.

### M2 Step Progress

| Step | State | Notes |
|------|-------|-------|
| S0 — Layer-origin refactor | ✅ done | 2026-04-20; 141 tests (+11); `LayerBase.originX/originY`, `PixelLayer::renderTile` origin-aware + mask pre-multiply, `CropCommand` overlap math + drop-to-empty, `.txl` v2 with per-layer dims + origin (v1 readable), `Document::addPixelLayer(img, ox, oy, name)` |
| S1 — Place image (`File → Place…`) | ✅ done | 2026-04-20; 145 tests (+4 in `test_place_image`); Ctrl+Shift+P opens PNG into the **current** doc; origin centered in doc (integer floor div); `LayerOpCommand` closure with shared stash (detach/reattach) so undo pops and redo reattaches the exact same layer instance; oversized PNGs keep offscreen pixels via S0 origin plumbing |
| S2 — Move tool | ✅ done | 2026-04-20; 151 tests (+6 in `test_move_layer`); V shortcut + ToolsPanel button + `Qt::SizeAllCursor`; live drag writes origin directly onto the active layer so the existing dirty-rect path partial-recomposites union of old+new doc footprints; `MoveLayerCommand(layerId, before, after)` looks up by id (safe against reorder between drag and later redo); zero-delta drags produce no undo entry |
| S3 — Free Transform | ✅ done | 2026-04-20; 176 tests (+25 across `test_resample`, `test_transform_command`, `test_transform_tool`); `src/geom/Affine2D` + `src/geom/Resample` (bilinear, premultiplied, 1e-5 alpha noise floor to tame float precision in rotation); `LayerOverride` struct + `compose(tree, out, override)` / `compose(tree, out, rect, override)` render a substitute image at an override origin, masks deliberately skipped (tool pre-bakes); `TransformCommand(doc, layerId, before, after, beforeOrigin, afterOrigin)` looks up by id + assigns whole `image` + swaps origin; `TransformTool` is modal (`enter`/`commit`/`cancel`/`isActive`), carries scratch image sized to doc-bbox AABB, `overlay()` reports active flag + `LayerOverride` + 4 corners; press hit-tests corner (scale, `kCornerHitRadius=10.f`) → inside quad (translate) → outside (rotate); `buildSrcToDoc` composes translate(−srcW/2,−srcH/2) → scale → rotate → translate(center); identity commit is a no-op. UI: `Edit → Free Transform` (Ctrl+T), ToolsPanel "T" button, `MainWindow::onTransformAccept` on Enter / `onTransformCancel` on Escape, tool-switch auto-commit via `commitTransformIfActive` in `setActiveTool`; `CanvasView::setTransformTool` + per-paint `overlay()` query feeds override to `compose()` and draws quad outline (dashed) + 4 corner dots. Shift aspect-lock / 15° rotation snap deferred to polish. |
| S4 — Lasso + Polygonal Lasso | ✅ done | 2026-04-20; 199 tests (+20 across `test_polygon_fill` and `test_lasso_tool`); `SelectionMask::fillPolygon` (even-odd scanline, half-open edge y-range, strict-interior edge rule with asymmetric floor/ceil so axis-aligned integer polygons stay predictable); `ToolBase` gains `livePath()` + `hover()` virtuals for click-driven modal tools; `LassoTool` (L) — press/move/release drag with 1-doc-px chord decimation, `<3` points collapses to Deselect on Replace or no-op otherwise; `PolyLassoTool` — click-appends vertices, `hover()` updates cursor so `livePath()` emits a trailing rubber-band edge, click within 6 doc-px of vertex 0 (with ≥3 verts) or `finish()` closes, `cancel()` discards; CanvasView adds black/white dashed polyline renderer + `widgetRectForPath` for tight invalidation; ToolsPanel gets L/P picker buttons + shared lasso options row (New/+/−/∩) so one persistent combine mode applies whichever lasso is active; MainWindow routes Enter/Escape to PolyLasso when Transform is idle and silent-cancels an in-progress polygon when the user switches tools mid-draw |
| S5 — Select by Color | ✅ done | 2026-04-20; 207 tests (+8 in `test_select_by_color`); `SelectByColorTool` click-samples the active pixel layer's color, walks every present tile (origin-aware, so off-doc layers still resolve), marks pixels within L∞ tolerance, commits a `SelectionCommand` with the shared combine-mode plumbing (modifiers-at-press win; else persistent mode); Intersect/Subtract use the existing selection as a traversal clip so the pick can't leak outside an active region; `channelDist` promoted from `FloodFill.cpp` (anon namespace) to `FloodFill.h` as an inline free function so wand + SBC share one metric; ToolsPanel adds a "Y" picker in the wand slot + reuses `wandGroup_` (header text swaps to "Select By Color" when active) with its tolerance slider and combine-mode buttons pushing into both tools; Shift+W cycles Magic Wand ↔ Select By Color in MainWindow. Uses `PixelLayer::image.tiles()` iteration to skip transparent holes entirely |
| S6 — Brush dynamics | ✅ done | 2026-04-20; 214 tests (+7 in `test_brush_dynamics`); `RoundBrushParams` adds `sizeJitter`, `opacityJitter`, `pressureMultiplier` (pressure stub for tablet, unwired in M2); `BrushEngine` owns a `std::mt19937` reseeded per stroke from `std::hash<uint64_t>{}(strokeId)` — monotonic counter by default, `setNextStrokeSeedForTesting` pins the next seed for deterministic replay; `applyStamp` branches on `sizeJitter > 0 \|\| opacityJitter > 0` so the base path stays bitwise identical to pre-S6; jittered path draws `U(-j,+j)` per stamp, scales + rounds diameter (capped at 2×D, floor 1), rebuilds kernel via new static `RoundBrush::buildKernel(D, hardness, out)`, scales opacity + clamps to [0,1]. ToolsPanel brush options row gains Size Jitter / Opacity Jitter / Spacing rows — spacing slider 1–100 % maps linearly to `spacingRatio ∈ [0.01, 1.0]`, all three sync back via `refreshFromBrush` so `[`/`]` or future keyboard edits stay visible |
| S7 — Verify + tag `v0.2.0-m2` | 🟨 user-verified 2026-04-21, tag pending | End-to-end walkthrough passed (Place → Move → Transform → Lasso/PolyLasso → Wand/SBC → jittered brush → `.txl` round-trip). User-facing bug caught: Free Transform Enter was reverting to original because `MainWindow::commitTransformIfActive` pushed the `TransformCommand` onto the undo stack without first calling `apply()` — `UndoStack::push` expects the side-effect to already be applied, and Transform's preview is an overlay (`LayerOverride`) so the real layer was untouched through the drag. Fixed in commit `4492355`: `cmd->apply()` is now called before `undoStack_->push(...)`; added `transform_pending_commit_writes_through_apply` integration test (215 tests green). Tag command ready — `git tag -a v0.2.0-m2 -m "M2 — Position, Shape, Stroke Quality"` — awaiting user sign-off for the tag itself |

## Previous Milestone: M1 — Selection, Fill, and Native Format ✅

**Definition of Done:** rectangular marquee + marching ants, bucket fill, magic wand, crop, `.txl` native file format round-tripping doc + layers + masks + selection. **Shipped 2026-04-20 as tag `v0.1.0-m1`.**

### M1 Step Progress

| Step | State | Notes |
|------|-------|-------|
| S1 — SelectionMask + brush clipping + Select menu | ✅ done | 2026-04-20; 75 tests passing (13 new in `test_selection`) |
| S2 — Rectangular marquee + marching ants | ✅ done | 2026-04-20; 86 tests passing (+9 marquee mode/edge cases, +2 persistent-mode); tool picker + B/M shortcuts; persistent combine mode buttons (New/+/−/∩) as WM-hijack-proof alternative to Alt modifier |
| S3 — Bucket fill + scanline flood | ✅ done | 2026-04-20; 97 tests passing (+11 in `test_fill`); G shortcut + options row (Tolerance 0–255, Opacity 0–100%); respects selection and paint target; PaintCommand undo |
| S4 — Magic wand (smart selection) | ✅ done | 2026-04-20; 104 tests passing (+7 wand/floodSelect); W shortcut; options row (Tolerance + combine-mode buttons); Shift/Alt modifiers + persistent combine mode; uses shared `floodSelect` |
| S5 — Crop tool + canvas resize | ✅ done | 2026-04-20; 117 tests passing (+13 in `test_crop`); C shortcut; drag-and-release commit; `CropCommand` deep-snapshot undo (per-layer tile clone + mask + selection); cropped selection collapses to null when it doesn't intersect |
| S6 — `.txl` native file format v1 | ✅ done | 2026-04-20; 130 tests passing (+13 in `test_txl_io`); custom chunked binary, tile-sparse, uncompressed v1; `File → Save As…` writes `.txl`, `File → Open…` dispatches on extension; round-trips dims/layers/masks/selection/active/paint-target |
| S7 — Verify + tag `v0.1.0-m1` | ✅ done | 2026-04-20; user verified end-to-end; post-verify follow-up: Photoshop-style cursors (Bucket/Wand/Crop pixmaps, Marquee cross, Brush ring); `TileRowCursor` tile-pointer caching in FloodFill + ants overlay walk + composite upload (fixes wand/bucket + undo latency on large images); first-pass app icon set under `assets/icons/` with Qt `.qrc` embed + `.desktop` + Windows `.rc` wiring; tagged `v0.1.0-m1` |

## Previous Milestone: M0 — Minimal Editor Bootstrap ✅

**Definition of Done:** Runnable Qt6 app that opens PNG, stacks pixel layers with opacity + 13 PS blend modes, paints with a round brush on layer or raster mask, undo/redo, exports flat PNG. **Shipped 2026-04-20 as tag `v0.0.1-m0`.**

## Step Progress

| Step | State | Notes |
|------|-------|-------|
| S1 — Bootstrap (apt install, git init, workflow docs) | ✅ done | 2026-04-20 |
| S2 — CMake + hello Qt window | ✅ done | 2026-04-20; `./build/tuxels` launches |
| S3 — Core: Tile, TileStore, TuxImage | ✅ done | 2026-04-20; 15 unit tests pass |
| S4 — Layers + compositor + 13 blend modes | ✅ done | 2026-04-20; 43 unit tests pass |
| S5 — UI shell: CanvasView + LayersPanel | ✅ done | 2026-04-20; window hosts sample 3-layer doc |
| S6 — PNG I/O + File menu | ✅ done | 2026-04-20; PngIO round-trip unit tests pass |
| S7 — Brush engine + BrushTool | ✅ done | 2026-04-20; 54 tests passing |
| S8 — Undo/redo with tile COW | ✅ done | 2026-04-20; 59 tests passing |
| S9 — Layer masks in UI | ✅ done | 2026-04-20; 59 tests passing |
| S10 — Verify + tag v0.0.1-m0 | ✅ done | 2026-04-20; ToolsPanel + partial-recompose + Command::dirtyRect undo path + brush cursor ring; user re-verified all 13 DoD items; tagged `v0.0.1-m0`; 62 tests |

## What Works Right Now

- `cmake -S . -B build -G Ninja && cmake --build build` produces `./build/tuxels`.
- Launching the binary opens a Qt6 main window with Photoshop-style menu stubs (File/Edit/Image/Layer/Select/Filter/View/Window/Help) and a status-bar "Ready".
- Verified headless: `timeout 2 ./build/tuxels -platform offscreen` runs for 2s without crash.
- Canvas shows a composited 1024×768 sample document (white bg, red rectangle, green disc blended Multiply); checkerboard around canvas; Ctrl+wheel zoom, middle/shift+drag pan.
- Layers dock (right) lists layers top-down with per-row visibility toggle, thumbnail, name, blend-mode combo, and opacity slider. Add/Delete/Up/Down via toolbar or Layer menu. Blend-mode or opacity changes trigger recomposite immediately.
- `File → Open…` imports an 8-bit PNG as a single pixel layer; `File → Export As PNG…` composites the document and writes an 8-bit sRGB PNG. PngIO round-trip verified in `test_png_io`.
- Click-drag on the canvas paints a round brush onto the active pixel layer. Brush kernel has hard/soft falloff (hardness ∈ [0,1]) and honors opacity × flow per stamp; spacing is diameter × 10%. `[` / `]` shrink/grow the brush diameter; status bar reports new size. Layer thumbnails refresh on stroke end.
- `Edit → Undo` / `Edit → Redo` (Ctrl+Z / Ctrl+Shift+Z) reverse and replay: paint strokes (via tile-COW snapshots — only touched tiles are recorded), add/delete/reorder layers, visibility toggles, blend-mode changes, and opacity slider commits (one entry per drag, coalesced on release). Undo depth: 64.
- `Layer → Add Layer Mask` attaches a white (fully-reveal) raster mask to the active pixel layer and switches paint target to mask. The layers panel shows a second thumbnail next to the layer thumb; click a thumb to choose paint target (layer or mask), shift-click the mask thumb to toggle enabled (disabled mask has a red tint overlay), right-click for "Delete Mask". All mask ops are undoable.
- **Tools dock (left)**: foreground/background color swatches (click → color picker, X swaps, D resets to black/white) plus size / hardness / opacity / flow sliders that drive the live brush. `[`/`]` and the dock stay in sync.
- **Paint latency fix**: `compose(tree, out, Rect)` overload restricts the tile loop to tiles touched by the current stamp; the canvas partial-recomposites and partial-uploads only those rows per mouse tick. Structural ops (layer add/delete/reorder, blend/opacity/visibility changes) still full-recompose; paint-stroke undo/redo goes through the partial path via `Command::dirtyRect()` which `PaintCommand` computes from its tile snapshot.
- **Brush cursor ring**: concentric black/white 1-px ellipses centered on the cursor, sized to the active tool's `cursorRadiusPx()` × zoom. Only the ring's widget rect is invalidated per mouse move (partial `update()`), so the preview is cheap. `[`/`]` refresh it live.
- **Selection model** (M1-S1): `core/SelectionMask` (1-channel over a document-sized `TuxImage`, tile-sparse fillRect/combine/invert/clone). `Document::selection()` is a nullable `unique_ptr<SelectionMask>`; null = "no selection". `BrushEngine` multiplies per-pixel deposit by `selection->sample(x,y)` so strokes are clipped to the active selection. `Select` menu wired: `All` (Ctrl+A), `Deselect` (Ctrl+D), `Inverse` (Ctrl+Shift+I) — each undoable via a dedicated `SelectionCommand`.
- **Marquee + ants** (M1-S2): ToolsPanel has a picker row with Brush (B) / Marquee (M) buttons. With the Marquee tool active, drag on the canvas builds a rectangular selection; modifiers at press time pick the combine mode — plain = Replace, Shift = Add, Alt = Subtract, Shift+Alt = Intersect. Live rubber-band dashed rect tracks the drag; on release a `SelectionCommand` commits the before/after pair (so the marquee is undoable). A 10 Hz `QTimer` animates the marching-ants overlay (black solid under white dashed, `dashOffset` rotates) around the selection boundary; only the selection's widget-space bbox repaints per tick. The Shift-for-pan gesture is suppressed when the Marquee is active, so Shift+Left adds to the selection instead of panning.
- **Marquee persistent mode** (M1-S2 follow-up): ToolsPanel reveals a Marquee options row with four combine-mode buttons (New / + / − / ∩) when the Marquee tool is active. The selected button drives the combine mode for drags with no modifier keys held at press time; holding Shift / Alt / Shift+Alt still temporarily overrides per Photoshop. This keeps Subtract and Intersect reachable on Linux WMs (GNOME default) that consume Alt-drag for window-move before Qt receives the event.
- **Paint bucket** (M1-S3): ToolsPanel picker row gains a Bucket (G) button; G shortcut switches tools. When the Bucket tool is active, a press on the canvas runs a 4-connected scanline flood fill at that pixel on the active paint target (layer or mask), comparing against the seed's source color with an L∞ tolerance threshold (UI slider 0–255, normalized to [0,1]). Fill is soft-masked by the selection (binary threshold for traversal; per-pixel multiply on the fill alpha for feathered edges) so bucket-inside-selection just works. A Bucket options row (Tolerance / Opacity) appears when the tool is active. The foreground swatch is the shared source of truth for both brush color and fill color. Fill tile-COW snapshots are wrapped in a `PaintCommand`, so each click is one undo step. Non-contiguous (same-color-anywhere) fills are deferred.
- **Magic wand** (M1-S4): picker row gains a Wand (W) button; W shortcut switches tools. A click on the canvas samples the active PixelLayer's pixel color, runs a scanline flood (`floodSelect` in `fill/FloodFill`) with L∞ tolerance, and commits a `SelectionCommand` carrying the combined before/after selection. Combine-mode semantics match the marquee (Replace/Add/Subtract/Intersect — modifiers win when held, persistent-mode buttons cover WM Alt-hijack). Subtract/Intersect further clip the wand traversal to the existing selection so the wand can never escape it. Shift-click on the canvas routes to the wand (not the pan gesture) because `consumesShiftClick()` returns true.
- **Crop** (M1-S5): picker row gains a Crop (C) button; C shortcut switches tools. A drag on the canvas defines the crop rectangle (rubber-band rendered via the new generic `ToolBase::liveRect()` virtual — shared with the marquee). On release with a non-degenerate rect the tool produces a `PendingCrop{rect}` that MainWindow converts into a `CropCommand`. The command's constructor deep-snapshots the pre-crop document (tile-clones of every PixelLayer image + any mask + the active selection), then calls `applyCropInPlace` which replaces each layer's image with a cropped copy, crops the selection mask to the new doc space (collapsing to null if nothing remains selected), and resizes the document. Undo re-installs the deep snapshot; redo re-runs the crop math. Degenerate drags (< 2 px in either axis) are ignored. Crop respects the document bounds by clamping in `CropTool::release`. Interactive Photoshop-style "drag → adjust handles → Enter to commit" is deferred; current behavior is drag-to-commit with Ctrl+Z to recover.
- **.txl native format** (M1-S6 → M2-S0): `io/TxlIO.{h,cpp}` in `tuxels_core` (pure C++, no Qt). Custom chunked little-endian binary, tile-sparse — writes only present tiles of each PixelLayer image, each mask (when attached), and the selection (when present). Round-trips doc dimensions, active layer, paint target, and per-layer metadata (id / name / visibility / mask-enabled / opacity / blend mode). Bumped to v2 in M2-S0 to record per-layer backing-image width/height and doc-coord origin; v1 files still load with origin=0 and layer dims == doc dims. `Document::setLayerIdCounter` seeds the id counter on load so freshly added layers don't collide with loaded ids. UI: `File → Save As…` (Ctrl+Shift+S) writes `.txl`; `File → Open…` filter includes `*.txl *.png` and dispatches on extension. `Export As PNG…` stays on its own menu entry for flattened exports.
- **Layer origin + mask-in-renderTile** (M2-S0): `LayerBase` owns `int originX, originY` (both default 0). `PixelLayer::renderTile` now translates doc tile coords into layer-local coords, intersects with the layer's backing-image rect, and samples — layers can live anywhere on the doc grid. Masks share their owning layer's origin + dimensions; mask sampling is pre-multiplied into the tile's output alpha inside `renderTile` so `compose()` itself stays origin-unaware. `Document::addPixelLayer(TuxImage&&, int ox, int oy, name)` is ready for S1's Place Image. `CropCommand` intersects each layer's doc-space content rect with the crop and translates origins into the new doc; layers with no overlap collapse to 0×0 with origin (0,0). Mask creation in `Add Layer Mask` follows the invariant by sizing masks to `layer->image.{width,height}()` instead of doc dims.
- **Place image** (M2-S1): `File → Place…` (Ctrl+Shift+P) opens an 8-bit PNG and adds it as a new layer on the **current** document (does *not* replace the doc or open a second window). The new layer's origin is centered in the doc via integer floor-div `((docW - imgW) / 2, (docH - imgH) / 2)`, so oversized PNGs hang past the doc edges with their offscreen pixels preserved in the layer's backing image — they become visible once Move, Crop, or doc-resize reveals them. Undo/redo uses the `LayerOpCommand` closure pattern with a shared `stash` that holds the detached layer between undo and redo, so the exact same `PixelLayer` instance round-trips through the history (no re-decode, no image realloc).
- **Move tool** (M2-S2): picker row gains a Move (V) button; V shortcut switches tools and installs `Qt::SizeAllCursor`. A drag on the canvas while Move is active shifts the **active** layer's `(originX, originY)` by the cursor delta — pixels don't change, the layer's backing image + mask are untouched. Live feedback comes for free from the existing dirty-rect path: each `MoveTool::move()` writes the origin directly and reports a union of the pre-move and post-move doc-space footprints as the dirty rect, so the canvas only recomposites the pixels that actually changed. On release a `MoveLayerCommand(layerId, beforeOrigin, afterOrigin)` lands on the undo stack — the command looks up the layer by id (not by index) so a later redo works even if layers have been reordered since the commit. Zero-delta drags produce no undo entry. Non-pixel active layers (none exist yet, but the check is there) are ignored rather than crashing.
- **Select By Color** (M2-S5): picker row gains a "Y" button in the wand slot (Shift+W cycles Magic Wand ↔ Select By Color, matching Photoshop's grouped-tool UX). A click on the canvas samples the active pixel layer's color at that doc-coord (translated to layer-local via origin), walks every present tile in that layer's backing image, and marks every pixel within L∞ tolerance of the seed — non-contiguously. Combine modes work identically to the marquee/wand: modifiers at press-time win (Shift = Add, Alt = Subtract, Shift+Alt = Intersect), else fall through to the shared wand options row. Intersect and Subtract use the existing selection as a traversal clip so the pick never leaks outside the active region. The L∞ `channelDist` helper is shared with the contiguous `floodFill` / `floodSelect` path — both tools' tolerance sliders mean the same thing. Commits as a `SelectionCommand` via the usual `PendingCommit`/`takeCommit` pop in `onLayerPainted`.
- **Lasso + Polygonal Lasso** (M2-S4): ToolsPanel picker row gains L (Lasso) and P (Polygonal Lasso) buttons; L shortcut switches to the freehand lasso. With Lasso active, a drag builds a polyline (1-doc-px chord decimation so slow drags don't balloon), release rasterizes it via `SelectionMask::fillPolygon` (even-odd scanline, half-open edge y-range `[yMin, yMax)` to avoid double-counting at shared vertices, strict-interior edge rule `xa < i+0.5 < xb` via `xStart = floor(xa + 0.5)` and `xEnd = ceil(xb - 0.5)`) and commits a `SelectionCommand` with the combined before/after pair. PolyLasso is click-vertex: each left click appends a vertex, `hover()` tracks the cursor between clicks so `livePath()` emits a trailing rubber-band edge to render, a click within 6 doc-px of vertex 0 (with ≥3 verts) closes, Enter also closes, Escape cancels. Combine modes work exactly like Marquee/Wand — modifiers at press-time win (Shift = Add, Alt = Subtract, Shift+Alt = Intersect), else fall through to the shared lasso options row (New / + / − / ∩) that applies to whichever lasso tool is active. Switching tools while a polygon is in progress silently discards it rather than committing an unclosed shape. Live polyline renders black underlay + white dashed overlay with per-event bbox invalidation via `widgetRectForPath`.
- **Free Transform** (M2-S3): `Edit → Free Transform` (Ctrl+T) or the ToolsPanel "T" button calls `TransformTool::enter(doc)`, which snapshots the active pixel layer's image (cheap shared_ptr-backed tile copy) + origin, zeros scale/rotation deltas, and sizes a scratch `TuxImage` to the doc-bbox AABB of the current transform. Drags on the canvas hit-test: near a corner (within `kCornerHitRadius = 10.f` doc-px) → Scale drag (un-rotates the cursor vector into the pre-rotation frame, then maps to new `scaleX/Y` using the corner's sign); inside the transformed quad → Translate drag (center += delta); outside → Rotate drag (atan2 delta around the center). Each move re-runs `rebuildScratch()` which recomputes corners, resizes the scratch image, and calls `resampleBilinear(src, scratch, dstLocalToSrc)`. `Resample` uses pixel-center sampling, premultiplies RGB by alpha before the 4-tap blend, and drops results whose alpha falls below a `kAlphaEps = 1e-5f` floor to suppress float noise from `std::cos(π/2) ≠ 0`. Preview compose: `CanvasView::setTransformTool` wires the tool; each paint queries `overlay()` (active flag + `LayerOverride{layerId, &scratch, originX, originY}` + 4 corner points) and passes the override into `compose()`, substituting scratch pixels for the real layer without mutating it — masks are deliberately skipped on overrides. The bbox quad (dashed white over black) + four 6×6 white corner dots render on top. Enter: `onTransformAccept` → `commit()` returns a `PendingCommit{layerId, before, after, beforeX/Y, afterX/Y}` (nullopt for identity → no-op); `TransformCommand` wraps it and pushes onto the undo stack; the tool returns to Brush. Escape: `onTransformCancel` → tool's `cancel()` discards the scratch; no undo entry. Tool switch via picker auto-commits via `commitTransformIfActive` in `setActiveTool` so a user who's clearly done can just pick another tool. `TransformCommand::undo/redo` assigns the whole stored `TuxImage` into the layer (tiles are refcounted shared_ptrs, so whole-image copies are cheap) + swaps origin; layer is found by id so a later tree reorder doesn't break redo. Shift-for-aspect-lock + 15° rotation snap deferred (nice-to-have polish).
- **Brush dynamics** (M2-S6): ToolsPanel brush options row gains **Size Jitter**, **Opacity Jitter**, and **Spacing** sliders below Flow. Spacing is 1–100 % (maps linearly to `spacingRatio ∈ [0.01, 1.0]`, default 10 %). At zero-jitter the paint path is byte-for-byte identical to the pre-S6 brush (the branch in `BrushEngine::applyStamp` short-circuits away from the RNG entirely), so the existing alpha-math tests carry over as a regression floor. With jitter > 0, `BrushEngine` reseeds a per-stroke `std::mt19937` from `std::hash<uint64_t>{}(strokeId)` at each `beginStroke` — the stroke id is a monotonic counter by default, pinnable via `setNextStrokeSeedForTesting(id)` for deterministic replay in tests. Each stamp draws `U(-jitter,+jitter)` and scales: effective diameter = `round(D·(1+u))`, floored at 1 and capped at 2×D (so big jitter doesn't blow up stamp cost); effective opacity = `op·(1+v)` clamped to `[0,1]`. Kernel is rebuilt per-stamp via the new static `RoundBrush::buildKernel(D, hardness, out)` into a scratch buffer owned by BrushEngine. `pressureMultiplier` rides on `RoundBrushParams` but is unwired in M2 — tablet integration is M3+ work and doesn't need to round-trip through the params for now.

## What Is Broken / Known Issues

- None known. `v0.2.0-m2` tag is the only outstanding M2 deliverable and is pending explicit user authorization.

## Open Questions

- See `ARCHITECTURE.md` "Open Questions" section. None currently blocking.

## For Cold-Start Claude

1. Read this file.
2. Read `NEXT.md` — exact next actions.
3. Read `ARCHITECTURE.md` — as-built decisions, don't re-derive.
4. Read `BUILD.md` if build-related.
5. Only after that, `SCOPE.md` for the big picture (long — skim table of contents).
6. The approved plan is at `/home/james/.claude/plans/cryptic-stargazing-moonbeam.md` (M2). M1 plan archive: `/home/james/.claude/plans/steady-framing-willow.md`.
